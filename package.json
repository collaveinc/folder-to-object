{
  "name": "@collaveinc/folder-to-object",
  "version": "1.0.1",
  "private": false,
  "description": "Requires all folder contents into an object.",
  "main": "lib/index.js",
  "scripts": {
    "compile": "coffee --compile --output lib src",
    "dev": "coffee tests/index.coffee",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Alegre (andrew.alegre@collave.com)",
  "license": "ISC",
  "dependencies": {
    "fast-glob": "^2.2.6"
  },
  "devDependencies": {
    "coffeescript": "^2.4.1"
  }
}
